<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="DialogTheme" parent="Theme.AppCompat.Light.Dialog">
        <item name="colorAccent">@color/blue</item>
        <item name="android:windowTitleBackgroundStyle">@style/DialogThemeWindowTitleBackground</item>
        <item name="android:windowTitleStyle">@style/DialogThemeWindowTitle</item>
    </style>

    <style name="DialogThemeWindowTitle">
        <item name="android:textSize">18sp</item>
        <item name="android:textColor">@color/blue</item>
        <item name="android:layout_marginBottom">8dp</item>
    </style>

    <style name="DialogThemeWindowTitleBackground">
    </style>

    <style name="DialogThemeMR" parent="DialogTheme">
        <item name="colorAccent">@color/purple</item>
    </style>

</resources>
